# Структура і прынцыпы праекта «v-ice part» (geometry-first вектарызатар)

> Складзена 2026-07-11 на аснове поўнага аўдыту кода (10 паралельных чытачоў + крытык-правяральшчык).
> Мэта праекта: **перагнаць vectorizer.ai па перцэптыўнай якасці вектарнага выніку** —
> ідэалізаваныя, сіметрычныя, безартэфактныя формы; малыя іконкі, дробныя дэталі і тэкст — усё выдатна.

---

## 1. Агульная карціна

Праект — даследчы raster→vector канвеер, пабудаваны вакол **Hoshyari et al. 2018
«Perception-Driven Semi-Structured Boundary Vectorization»** (`papers/perception-driven-vectorization.pdf`),
пашыраны ўласнымі ідэямі: MiniNet 4× subpixel-рэканструкцыя, ICM-ачыстка колераў, аднаўленне
схаваных слаёў (occlusion completion), градыенты, stroke-дэтэкцыя, region graph без швоў,
і вялікі блок глабальнай рэгулярызацыі (восі, паралелі, сіметрыя, ко-цыркулярнасць, базавыя лініі).

**Ядро** — `geometry_vectorizer.py` (4225 радкоў, ~215 КБ). Усё астатняе — дэтэктары вуглоў,
датасеты, бенчмаркі, прэв'ю — абслугоўвае яго.

**Важна:** `.git/` пусты (няма HEAD, аб'ектаў, гісторыі). Уся гісторыя цюнінгу жыве ў
125 каталогах `test_runs/` і снапшотах `benchmarks/`. Варта зрабіць `git init` + першы каміт.

---

## 2. Кропкі ўваходу

| Кропка | Што робіць | Рэжым па змаўчанні |
|---|---|---|
| `run_vectorizer.py` | CLI: файлы/папкі/глобы → `process()` на кожны рэжым → contact sheet | `--smoothing perceptual` (!), `--extractor mininet` |
| `Run-Vectorizer.ps1` | Абгортка з правільным інтэрпрэтатарам `C:\Python312\python.exe` (torch+CUDA) | — |
| `web_preview/server.py` | HTTP 127.0.0.1:8877, drag&drop UI, + API рэв'ю corner-датасетаў | `smoothing="cad"` (!) |
| `preview.py` / `Start-Preview.ps1` | Аўтапошук інтэрпрэтатара, аўтарэстарт сервера пры змене *.py | — |

⚠️ **Ні адна кропка ўваходу не запускае па змаўчанні флагманскі `paper`-рэжым.** CLI-дэфолт
`perceptual` — гэта інтэрпаляцыйны рэжым без прымітываў і ідэалізацыі (адзін кубік на ~0.5px!),
а web-дэфолт `cad` — legacy. Пры дэфолтных наладах CNN вуглоў і ўвесь Hoshyari-фіттэр наогул не запускаюцца.
Апрача таго, CLI-whitelist не прапускае `paper-native`/`paper-perc`/`paper-perres` (даступныя толькі праз web ці прамы выклік `process()`).

### Рэжымы smoothing (у `process()`)

- **Флагманы:** `paper` (прадакшн: deblur+ICM+merge+поўны Hoshyari-фіт), `paper-regions`
  (+Sec-7 region graph, фіт агульных межаў адзін раз — без швоў), `paper-native` (кананічная
  рэпрадукцыя: без deblur/ICM/merge), `paper-perc` (фіт па Taubin-згладжаным контуры, px=0.6),
  `paper-perres` (RF-дэтэктар вуглоў па рэзалюцыях, як у пэйперы).
- **Legacy:** `cad`, `uncertainty`, `perceptual`, `perceptual-icm`, `perceptual-merge`, `none`,
  `corner`, `chaikin`, `bspline`.

---

## 3. Канвеер (рэжым `paper`, ад PNG да SVG)

```
Раster (PNG/JPG)
 │ 1. Роутынг па класе шуму: JPEG → native raster (px=1.0);
 │    lossless ≤512px → MiniNet deblur_4x (4×, snap_palette=False)
 ▼
 │ 2. compact_palette(source)  ← subpixel_mininet.py: median-cut 16 → зліццё
 │    hue-сем'яў → agglomeration па ΔE/таўшчыні → выратаванне акцэнтаў
 │    (АНКЕРЫ = фінальныя колеры SVG! пераацэнкі па зыходніку няма)
 ▼
 │ 3. Пазначэнне пікселяў: argmin ΔE (OpenCV-Lab) да анкераў; фон = анкер,
 │    бліжэйшы да медыяны рамкі → ВЫКІДАЕЦЦА (у SVG — белы rect)
 ▼
 │ 4. Ачыстка: _icm_labels — Potts MRF (λ=45, σ=36, 14 ітэрацый checkerboard ICM,
 │    contrast-sensitive weights) → _merge_regions (union-find, слівэры па
 │    таўшчыні/ΔE<80) з імунітэтам _enclosed_counter (дзіркі літар любога памеру)
 ▼
 │ 5. Маскі: connectedComponents на анкер; фільтры плошчы/extent + выратаванне
 │    замкнёных контураў (area≥2)
 ▼
 │ 6. Структурныя пра-пасы: _merge_gradient_stacks (палосы → 1 маска + SVG-градыент),
 │    _complete_occlusions (схаваны rect/эліпс → шаблон ПАД усім; сакрэт VAI!),
 │    _detect_stroke (стужка пастаяннай шырыні → цэнтральная лінія + stroke-width)
 ▼
 │ 7. Контуры: mask_loops (vectorize_papers.py) — абход рэбраў пікс. ячэек,
 │    гарантавана замкнёныя петлі, знешнія CW / дзіркі CCW
 ▼
 │ 8. Вуглы (Sec 4): 1D dilated CNN па signed-turning сігнале (7 акон 1..21px),
 │    гібрыдны роутынг: corner_cnn_hybrid_large_v1.pt @0.36 (extent>4px, density≤8)
 │    else corner_cnn.pt @0.28; NMS 2px; фолбэк RF → recenter → collapse пар →
 │    _iterated_removal (Alg 2: E=α·D+R, α=32/extent, лексікаграфічна)
 ▼
 │ 9. Фіт сегментаў (Sec 5.1, fit_segment_midpoints): shortest-path DP па
 │    сярэдзінах пікс. рэбраў з ІНТЭРВАЛЬНЫМ абмежаваннем (±0.5px уздоўж нармалі
 │    рабра + eps 0.1) — лесвіца сцягваецца ў адну лінію БЕЗ згладжвання;
 │    прымітывы: line(r=1) / arc(r=2, Taubin fit_circle) / КЛАТОІДА(r=4, LSQ у
 │    tangent-angle + Пракруст) / biarc / cubic; стан DP = (вузел, 12 бінаў датычнай);
 │    G1-штраф 6.0 за >4°; Alg-1 repair (бан парушальнікаў, ≤3 паўторы);
 │    вуглы = перасячэнне аналітычных апор (line∩circle і г.д., cap 1.5px)
 ▼
 │ 10. Бэкстоп _finish_loop: двухбаковы Хаўсдорф ≤ max(2.5, 2px+1), fill-IoU ≥0.90
 │     (толькі тоўстыя), тэст самаперасячэння → пры правале ЎВЕСЬ луп замяняецца
 │     піксельнай ламанай ~2px («-fallback» — найгоршы магчымы выгляд!)
 ▼
 │ 11. Глабальная рэгулярызацыя (Sec 6, _regularize_regions_global): восі (8°→0/90),
 │     паралельныя кластары па ўсёй карцінцы (6°), артаганальнасць (4°→90°),
 │     агульныя carrier-лініі (1.2px), ко-цыркулярныя дугі (2%/4%), люстэрка
 │     (толькі верт./гарыз. восі, residual ≤0.8px), N-fold ратацыя (12..3),
 │     паўторы формаў, базавыя лініі гліфаў; snapshot+revert пры дэвіяцыі >3px
 ▼
SVG: write_svgs — адзін <path fill-rule="evenodd"> на рэгіён (дзіркі ў тым жа path),
z-парадак = плошча па змяншэнні (occlusion-шаблоны ў самым нізе), белы фонавы rect,
каардынаты %.2f, дугі як кубікі (без каманд A) + PNG-прэв'ю + report.json
```

`paper-regions`: калі рэгіёны датыкаюцца (≥16 агульных крэкаў і ≥8% мяжы) — замест
кроку 7-9 будуецца **crack-graph** (`region_graph.py`, Kopf-Lischinski): кожны інтэрфейс
фітуецца АДЗІН раз і выкарыстоўваецца абодвума рэгіёнамі (шво немагчымае канструктыўна),
кансэнсус вуглоў з двух бакоў, junction pinning у адну кропку, ко-цыркулярнае аднаўленне
разрэзаных колаў (Mastercard: 223→191 прымітываў, IoU 0.9901).

---

## 4. Модулі

### Ядро
| Файл | Радкоў | Роля |
|---|---|---|
| `geometry_vectorizer.py` | 4225 | Усё: экстракцыя масак, ICM, merge, градыенты/строўкі/аклюзіі, вуглы, DP-фіттэр, клатоіды, рэгулярызацыя, SVG. Адзіная кропка — `process()` |
| `vectorize_papers.py` | 895 | Агульная геам. бібліятэка: `mask_loops` (тапалагічна бездакорны трэйсер), `fit_circle` (Taubin 1991+Newton), `circular_beziers` (4/3·tan(θ/4)), Taubin/Chaikin/B-spline згладжванне + legacy Favreau-DP і «polyvector» (frame-field — мёртвы код) |
| `region_graph.py` | 630 | Sec-7 crack-graph: fit-once інтэрфейсы, saddle resolution, junction pinning, ко-цыркулярнае аднаўленне |
| `subpixel_mininet.py` | ~550 | MiniNet 4× SR (156k параметраў, residual+PixelShuffle, bounded 0.55·tanh) + **compact_palette** — самы ўплывовы рычаг колеру ва ўсім праекце |

### Вуглы (ML)
| Файл | Роля |
|---|---|
| `corner_cnn.py` | Прадакшн-дэтэктар: 5-слаёвы 1D dilated CNN (циркулярны padding, receptive field ~±90px), уваход — 7-канальны signed turning (вокны 1,2,3,5,8,13,21px), tiling кароткіх лупаў (<67 вяршыняў) |
| `corner_classifier.py` | RF-фолбэк (56-dim offset-фічы ×8 dihedral + turning/saturation/log-res) + УСЕ генератары трэніровачных даных (сінтэтыка, корпусы, тэкст, негатывы, human, paper) |
| `corner_perres.py` | Пэйпер-дакладны банк з 6 RF па рэзалюцыях (толькі Hoshyari GT) — навуковы бэйзлайн |

### Датасеты / GT (пакаленні V2→V4)
| Файл | Роля |
|---|---|
| `svg_corner_gt.py` | Дакладныя структурныя C0-вуглы з аналітыкі SVG + paint-order occlusion-мадэль + short-span канвенцыя (абодва канцы 1-2px пляцоўкі = адна падзея) |
| `svg_corners.py` | Legacy windowed-turning/saturation/acute лэйблер (IPAN99-сям'я) |
| `build_corner_dataset.py` | NPZ-шарды + маніфест + убудаваны review-UI (пункт/span/аклюзія/гумка, undo) |
| `build_combined_corner_dataset.py` | V4-камбайн: недатыкальны слой Hoshyari human GT (157 лупаў/1254 лэйблы) + svg-слой праз review.json |
| `annotate_corners.py` | Ручная анатацыя (resvg-рэндэр, нармалізаваныя каардынаты, snap да turning-max) |
| `paper_data.py` | Лоадэр Hoshyari GT з `C:/Users/nirrt/Toolset/Paper_training` |
| `generate_primitive_corpus.py` | 19 сем'яў × 4 класы падзей (smooth/structural/mixed/occlusion) |
| `generate_glyph_pilot.py`, `generate_rotated_glyph_pilot.py` | Arial-гліфы; ратацыі × субпіксельныя фазы (anchor-subpath трук захоўвае фазу) |
| `export_to_paper_format.py` | Экспарт нашых анатацый у фармат label_ui.py пэйпера |

### Эксперыменты па-за пайплайнам
- `font_match.py` (876 р., актыўна) — font retrieval для вядомага тэксту ў ROI: рэндэр кожнага
  шрыфта, пошук па tracking+affine, скор 0.55·IoU + 0.35·exp(−12·chamfer) + 0.10·topology,
  кансерватыўны gate, **preview-only, у пайплайн НЕ ўключаны** (тэкст пакуль не замяняецца гліфамі).
- `vocabulary_primitives.py` — DEPRECATED (форензіка даказала: CSV-квоты каманд VAI — гэта
  статыстыка гатовага SVG, а не параметры алгарытму).

### Харнэс (бенчмаркі/тэсты)
| Файл | Роля |
|---|---|
| `benchmark_stages.py` | Галоўны рэгрэсійны гейт: 12 сінтэтычных формаў + 13 behavior-проб (градыент так/сцяг не, капсула-строўк/тоўсты-rect-не, аклюзія так/L-не, сіметрыя шчыта ≥0.98) + 11 праблемных лагатыпаў з падлогамі IoU + corner-F1 ≥0.68; snapshot з дэльтамі, exit 1 |
| `compare_corner_detectors.py` | Сумленная event-level метрыка дэтэктараў (span = 1 падзея) |
| `compare_corner_pipeline.py` | Тая ж метрыка ПАСЛЯ поўнага postprocess па 3 палітыках |
| `benchmark_focused_synth.py` | Held-out генералізацыя: Segoe UI Light (трэніроўка на Arial), 300 прымітываў, 300 smooth-негатываў |
| `fit_benchmark.py` | Геаметрыя фіттэра: дэвіяцыя + corner P/R на растэрызаваных GT SVG |
| `test_*.py` (9 шт.) | Пінуюць: GT-лэйблер, batched-CNN == per-item, палітыкі postprocess, fit-profile гейты, tiny-тапалогію, генератары, font_match |

---

## 5. ML-мадэлі: што РЭАЛЬНА грузіцца

У `models/` ~40 файлаў, але інферэнс кранае толькі **тры**:

1. `corner_cnn.pt` (266 КБ) — малыя/шчыльныя лупы, парог 0.28
2. `corner_cnn_hybrid_large_v1.pt` — звычайныя лупы (extent>4px, density≤8 в/px), парог 0.36
3. `subpixel_mininet_4x.pt` — deblur_4x

Фолбэкі: `corner_rf.joblib` (109 МБ, толькі пры збоі CNN, і ён мicкалібраваны — клэмп
[0.50,0.60] пры HPO-оптымуме ~0.65), `corner_perres_paper.joblib` (толькі рэжым paper-perres).
Астатнія ~35 файлаў — бэкапы і непрамоўтнутыя кандыдаты (гісторыя трэніровак).

Актуальны стан дэтэктара (лог 2026-07-11): event-F1 ≈ **0.73–0.74** — гэта галоўны
перцэптыўны столь; сертыфікаваны гейт: Mastercard IoU 0.9901, Mobil 0.9849, NBC 0.9855,
Lacoste 0.9380.

---

## 6. Прынцыпы праекта (як ён «думае»)

1. **Geometry-first, не ML-first.** ML толькі там, дзе геаметрыя не мае адказу (перцэпцыя
   вуглоў, subpixel-рэканструкцыя). Уся вектарызацыя — дэтэрмінаваная аптымізацыя.
2. **Недакладны прымітыў ніколі не адгружаецца.** Абарона ў глыбіню: інтэрвальнае
   абмежаванне ў DP → Alg-1 re-check → двухбаковы Хаўсдорф + IoU + самаперасячэнне на
   узроўні лупа → snapshot+revert вакол глабальнай рэгулярызацыі.
3. **Перцэпцыя маштабу.** «Вугал» вызначаецца ў ВЫХАДНЫХ пікселях: той жа філет — вугал
   пры 256px і гладкасць пры 32px. Усе парогі GT — у пікселях аперацыйнага маштабу.
4. **Ідэалізацыя абмежавана бюджэтам дакладнасці.** Кожны snap (вось, паралель, сіметрыя)
   гейціцца: length·sin(dev) ≤ 1px, рух вяршыні ≤ 2px і г.д.
5. **Семантычная структура выхаду.** Градыент — адзін gradient-fill; стужка — stroke з
   шырынёй; схаваная база — поўны шаблон пад усім (галоўны ўрок форензікі VAI).
6. **Тапалогія святая.** Дзіркі літар (2×2px!) ратуюцца тапалагічным крытэрыем (адзін
   сусед + кантраст + не мяжа), а не плошчай. Kissing shapes не зліпаюцца (right-turn
   priority у mask_loops).
7. **Эксперыментальная дысцыпліна.** Кожная канстанта анатавана правалам, які яна
   выпраўляе; кандыдаты не прамоўцяцца без гейта; рэгрэсіі адкатваюцца (subpixel-RDP
   адкацілі, бо tiny-colour 0.4225→0.3772).
8. **Праўда пра GT.** Лэйблы — з аналітыкі SVG (нуль цыркулярнасці), human-слой Hoshyari
   недатыкальны, review-праўкі чалавека — паўнавартасныя трэніровачныя лэйблы.

---

## 7. Дзе выйграецца/прайграецца якасць (галоўныя рычагі)

1. **`compact_palette`** — самы верхні рычаг: анкеры = і тапалогія рэгіёнаў, і ФІНАЛЬНЫЯ
   колеры SVG (пераацэнкі няма). Памылка тут неаднаўляльная ніжэй па плыні.
2. **Роутынг deblur/native** (≤512 lossless → 4×; JPEG → native): няправільны выбар =
   альбо страта субпіксельных дэталяў, альбо хваляванне межаў.
3. **Набор вуглоў** (CNN 0.28/0.36 + recenter + collapse(16·spacing!) + removal):
   кожная мяжа сегмента, C0-кропка і ўся сіметрыя паходзяць адсюль. Post-removal recall
   ~0.73 — прызнаны разрыў на дробным тэксце.
4. **Інтэрвальнае абмежаванне px** (0.5px паўшырыня ўздоўж нармалі) — адзіная ручка
   «лесвіца сцягваецца ↔ дэталь захоўваецца». px=1.0 нават на 4×-deblur уваходзе.
5. **`_finish_loop` гейты** — рашэнне «прыгожы фіт ці піксельная ламаная». Вядома, што на
   тэксце прадакшн-IoU-гейт замяняў валідныя 5-8-прымітыўныя гліфы на 29-86 лесвічных
   лініяў; фікс `text-safe` існуе, але НЕ ўключаны.
6. **Гейт region graph** (≥16 крэкаў): калі не прайшоў — суседнія рэгіёны фітуюцца
   незалежна → магчымыя валасяныя шчыліны (клас артэфактаў, якога ў VAI няма).
7. **Sec-6 талеранцы** (0.8px сіметрыя, 1px восі, 2%/4% ко-цыркулярнасць) — уласна
   «вось ідэалізацыі», па якой судзяць супраць vectorizer.ai.
8. **Палітыка tiny-лупаў** (≤18px² → эліпс ці літаральны піксельны ланцуг) — якасць малых
   іконак упіраецца сюды: нічога паміж «ідэальны эліпс» і «лесвіца» няма.

---

## 8. Вядомыя слабасці і даўгі (сабрана аўдытам)

### Крытычныя для мэты «перагнаць VAI»
- **Швы паміж рэгіёнамі**: fit-once толькі ў `paper-regions` і толькі пры кантакце ≥16px;
  інакш — незалежны фіт агульных межаў (двойчы!) з рызыкай шчылін/нахлёстаў.
- **Fallback = лесвіца**: любы збой бэкстопа адгружае сырую піксельную ламаную — перцэптыўна
  найгоршы вынік; другога шанцу з паслабленым слоўнікам няма.
- **Няма G2**: толькі G1 (паварот хэндлаў). Крывізна скача на стыках дуг/клатоід/кубікаў —
  VAI-выхад гладзейшы. Клатоідны drift размазваецца ЛІНЕЙНА (выходзіць з сям'і клатоід).
- **Тэкст**: font_match не падключаны; tiny-safe/text-safe палітыкі (вядомыя фіксы дробнага
  тэксту) выключаны; collapse-акно 16·spacing сцірае сапраўдныя суседнія вуглы літар.
- **Няма эліптычнай дугі ў DP** — толькі акружнасці; часткавыя эліпсы = пэчворк кубікаў.
- **Сіметрыя абмежаваная**: люстэрка толькі верт./гарыз., ратацыя толькі вакол цэнтроіда;
  дыяганальныя восі і зрушаныя цэнтры не знаходзяцца.
- **Празрыстасць**: native-шлях і compact_palette НЕ кампазіцяць alpha (толькі deblur_4x
  кампазіціць на белым) → анкеры могуць атруціцца смеццем пад alpha=0; у SVG заўсёды
  белы непразрысты фон.
- **Колеры = анкеры**: без фінальнай пераацэнкі па арыгінальных пікселях.

### Тэхнічны доўг
- `.git` пусты — няма version control.
- 01_contour.png і 02_primitive_map.png — ідэнтычны рэндэр двойчы (капіпаст-баг + марная праца).
- ICM праз np.roll — краі загортваюцца (супрацьлеглы бок як сусед); ICM лакальны (не graph-cut).
- Мёртвы код: pixel_graph/trace_graph (Bessmeltsev — толькі назва), cad_regularize_ring/signed_area імпарты, biarc амаль выцеснены клатоідай.
- CLI не прапускае 3 рэжымы, якія server.py прапускае; дэфолты CLI/web розныя.
- Жорсткія абсалютныя шляхі на сайдкар-праекты; знікшы файл = маўклівы SKIP гейта.
- `tiny_color_detail_score` вяртае 1.0 пры ЛЮБЫМ выключэнні (маўклівы pass уласнага гейта).
- V4 svg-слой пазначаны «analytic-plus-review», але review.json для яго не існуе — чыстая эўрыстыка.
- GT-растэрызатар ігнаруе fill-rule (XOR/even-odd заўсёды), хоць гліфы — nonzero; без AA/шумавой аўгментацыі.
- Бенчмаркі не мераюць супраць VAI і не маюць перцэптыўных метрык (акругласць, роўнасць крывізны, wobble) — сістэмная эстэтычная рэгрэсія праходзіць усе гейты.
- fit_benchmark мерае дэвіяцыю толькі fit→boundary (прапушчаны серыф не караецца).

---

## 9. Знешнія залежнасці (па-за рэпазіторыем)

| Шлях | Што там |
|---|---|
| `C:/Users/nirrt/Toolset/v-ize train/dataset` | Трэніровачны корпус: iconify-іконкі, 150k wordmark'аў `text_shapes.jsonl` (і база шрыфтоў для font_match) |
| `C:/Users/nirrt/Toolset/v-ice pictures/` | Праблемныя кейсы + ground truth (365 казіно-лагатыпаў) |
| `C:/Users/nirrt/Toolset/Paper_training/` | Рэліз Hoshyari GT (boundary.txt + corners.txt, 8-256px) |
| `C:\Python312\python.exe` | Адзіны інтэрпрэтатар з torch+cv2 (codex-runtime python іх не мае) |

Бібліятэкі: numpy, PIL, cv2, scipy (cKDTree), skimage (skeletonize, CIEDE2000), torch,
sklearn (RF), fontTools, svgpathtools, resvg_py.

---

## 10. Стан адносна vectorizer.ai (форензіка)

З `VAI_FORENSICS_RU.md` (IKEA 106×45):
- VAI: MAE 4.66 / RMSE 9.95; наш стары SVG: 41.77 / 89.93; наш geometry-first: **5.88 / 14.21**.
- Сакрэты VAI: (1) **аднаўленне схаваных слаёў** (поўны прамавугольнік за эліпсам),
  (2) глабальная аптымізацыя шаблонаў і ўзроўняў літар, (3) нуль каманд `A`/`ellipse` —
  усё кубікамі з каардынатамі 0.01px, (4) перамалёўка літар паверх слаёў.
- Наш пайплайн ужо робіць (1) і часткова (2); разрыў — у G2-гладкасці, тэксце, швах,
  паўнаце сіметрый і частаце лесвічных фолбэкаў.
