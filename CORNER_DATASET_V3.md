# Corner dataset V3: paper human + icons

Гэта не новая аўтаразметка замест датасэта paper. V3 захоўвае два асобныя пласты:

- `paper-human` — усе 157 арыгінальных boundary-прыкладаў і ўсе 1254 human labels без пераінтэрпрэтацыі;
- `svg-extended` — 80 розных SVG-іконак/лагатыпаў у 8 маштабах, з structural і occlusion labels.

Файлы датасэта: `datasets/corner_gt_v3_paper_plus_icons/`.
Вэб-рэв'ю: `http://localhost:8877/corner-dataset-v3/`.

## Як правяраць і выпраўляць

1. Націснуць на картку або `fullscreen`. Усе маштабы гэтай іконкі адкрыюцца на ўвесь экран.
   У загалоўку з'явяцца slider, `− / +` і бягучы zoom: ад 1% да 800%. Памер кропак і таўшчыня
   контуру застаюцца пастаяннымі ў экранных пікселях, таму яны не закрываюць 8–24 px іконкі.
2. Прапушчаны сапраўдны вугал: выбраць `+ structural` і націснуць на найбліжэйшы vertex контуру.
3. Вугал ад перакрыцця двух shapes: `Shift+click` або інструмент `+ occlusion`.
4. Лішняя кропка на гладкай дузе, скругленні ці raster-лесвічцы: правы клік або `remove`.
5. `approve` — толькі калі ўсе паказаныя resolution выглядаюць правільна. `reject` — калі няправільная сама тапалогія/рэндэр, а не адна кропка.
6. Націснуць `Save review`. Праўкі запішуцца ў
   `datasets/corner_gt_v3_paper_plus_icons/review.json` і будуць аўтаматычна ўжытыя loader-ам.

Памылковую праўку можна вярнуць праз `Ctrl+Z` або кнопку `Undo`. Крок наперад —
`Ctrl+Shift+Z`, `Ctrl+Y` або `Redo`. У fullscreen гэтыя кнопкі таксама ёсць побач з zoom.

Кнопка `raw` каля кожнага resolution адкрывае inspector: злева вялікі annotated bitmap,
справа raw без разметкі ў дакладным памеры (`32×32`, `64×64` і г.д.). Hover па вялікім
bitmap паказваецца жоўтай кропкай на маленькім raw і не змяняе разметку.

`Esc` або паўторны клік `fullscreen` закрывае поўнаэкранны рэжым. Фільтры `paper human`,
`SVG extended`, `без вуглоў`, `аклюзіі` дазваляюць хутка правяраць патрэбны клас.

## Навучанне

V3 ужо ўключае paper-human, таму нельга адначасова дадаваць `--paper`, інакш human-set
будзе прадубліраваны:

```powershell
python corner_cnn.py --train `
  --svg-gt datasets/corner_gt_v3_paper_plus_icons `
  --svg-gt-repeat 1 --paper 0
```

Split робіцца па сямействе/файле, а не па асобным resolution: адна і тая ж форма не можа
адначасова трапіць у train і validation. У manifest кожны файл мае `source_layer`, таму
метрыкі paper-human і svg-extended можна лічыць асобна.

## Правіла разметкі

Raster-лесвічка сама па сабе ніколі не з'яўляецца вуглом. Круг, эліпс, stadium і гладкая
Bezier-дуга застаюцца zero-corner на любым resolution. Пазітыў — гэта перцэптыўны разрыў
напрамку (structural) або бачны канец мяжы праз аклюзію (occlusion).
