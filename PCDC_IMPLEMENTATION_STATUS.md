# V-ICE PCDC — historical construction status (superseded)

> **Audit warning (2026-07-21):** this document previously treated module-level
> construction tests as proof that the corresponding production runtime path
> was complete.  The full plan-to-production audit found multiple critical
> disconnects: production bypasses dual-guided column generation, ProposalNet,
> the exact ROI court/atlas and mandatory proof bundles; the extractor omits
> several required global constraints; T3/T4 do not reach exported SVG; and
> Phase 12 has not run.  Phase “passed” labels below are retained only as a
> historical record and are not promotion evidence.  The authoritative current
> status is `PCDC_PLAN_TRACEABILITY_AUDIT.md`.

Canonical plan: `V-ICE_proof_carrying_design_compiler_plan_ru_v2.md`  
Plan SHA-256: `5FB2F288B44AD6764A1F99C5D915647AABF63220B1FE2395B50FC5433B714CBB`

This ledger records measured evidence. It never marks a phase complete from
machine suggestions or from unreviewed construction labels.

## Phase 0 — passed

- Production fallback remains V-ICE Best / `paper-regions`.
- Scene Engine remains an archived experiment and is not silently promoted.
- VAI50 and Challenge115 ledgers are complete: 50/50 and 115/115.
- Real Locus Corpus contains 300 unique owned/local raster sources across all
  six required semantic classes.
- The named stage profiler records failures and budget violations.
- Freeze ledger: `PCDC_BASELINE_FREEZE.json`.

## Phase 1 — passed on all 300 loci (revalidated after GT adapter v4)

REIR implements canonical color/alpha evidence, one fixed image-formation
posterior, multiscale boundary evidence, UCM hierarchy, inclusion trees, core
cells, uncertain boundary bands, half-edge interfaces, typed proposals,
immutable validation and a content-addressed cache.

Human/objective evidence is complete: 205 source-GT-derived loci and 95
human-reviewed loci; 0 pending and 0 fabricated reviews. All 205 derived rows
were regenerated by `owned-svg-ground-truth-adapter/v4`; the rerun rejected
zero owned SVGs. Opaque text/logo canvases use RGB foreground support rather
than treating full-canvas alpha as one fake glyph.

Canonical Experiment 1 report: `benchmarks/pcdc_experiment1/report.json`.

- overall Support Recall@32: `99.7364%`;
- class floor: `99.5199%`;
- boundary-band feasibility: `99.0539%`;
- macro-family recall: `99.6667%`;
- runtime p95: `157.03 ms`;
- topology recall: `81.6667%` (explicit warning; not a supplied Phase-1 gate).

## Phase 2 — passed

Implemented CMIR atomic/legacy/hierarchy/typed columns, exact ownership
bitsets, symmetric conflicts, hierarchy DP, conflict components, bounded local
exact extraction, always-feasible fallback, visible-scene validation, dual
master, seven pricing oracles and three-round bounded column generation.

Experiment 1B report: `benchmarks/pcdc_experiment1b/report.json`.

- acceptable missing-macro recovery within 3 rounds: `98.3333%`;
- final family recall: `99.6667%`;
- class floor: `96.6667%`;
- pricing p95 per active oracle/ROI: `7.80 ms`;
- generated/exhaustive registry ratio: `0.7170%`;
- no class uses a manual risk threshold;
- master remained feasible on 300/300 loci.

Experiment 2 report: `benchmarks/pcdc_experiment2/report.json`.

- exact/acceptable oracle scene: `99.6667%`;
- topology catastrophes: `0`;
- median solve: `2.80 ms`;
- p95 solve: `14.97 ms`;
- fallback feasible: `100%`.

One row reached the bounded solve budget and returned a valid best-so-far;
this is recorded as a timeout in the report and is not hidden by the gate.

## Phase 3 — passed

Implemented:

- immutable 1–8 hypothesis renderer posterior shared by both sides;
- true ROI-bounded signed-distance discrepancy;
- deterministic supersampled exact ROI atlas with explicit pixel budgets;
- validity, support, topology, geometry, render-evidence, structural,
  complexity and resource certificates;
- sealed certificate digests and CMIR proof binding;
- fixed-posterior local court in the required order, with exact tie → fallback;
- foundational Experiment 3 over all seven required pair types.

Experiment 3 report: `benchmarks/pcdc_experiment3/report.json`.

- construction-labelled machine pairs: `224/224` correct (`100%`);
- true microdetail recall: `100%`;
- topology catastrophe choice rate: `0%`;
- calibration ECE: `0.000000584` (target `<=0.05`);
- machine gate: passed;
- blind human reviews: `35/35`;
- human agreement: `100%` (gate `>=90%`);
- full Phase-3 gate: passed.

The 35-pair blind UI served live SVG A/B and native 64×64 evidence. The public
API withheld every private correct-side label; the resulting agreement was
computed only after all genuine local-human answers had been persisted.

## Phase 4 — passed

Implemented parallel REIR-direct line proposals, strict exact-font admission,
font-free SDF/dual-loop geometry, bounded repeated-glyph EM, TextLine CMIR
columns, legacy fail-open selection and lazy materialisation after the local
court. Losing line hypotheses carry immutable support certificates but do not
decode per-glyph loops or prototypes. OCR-backed exact-font retrieval is a
bounded optional anytime refinement after the valid T2 return: only candidates
that pass the strict silhouette/topology wall may re-enter the court.

Canonical Experiment 4 report: `benchmarks/pcdc_experiment4/report.json`.

- real line corpus: `100`;
- Glyph Catastrophe Rate reduction: `76.3994%` (gate `>=70%`);
- counters/components no worse: `100/100`;
- warm valid-return p95: `159.71 ms/line` (gate `<200 ms`);
- machine gate: passed;
- blind human preference court: `100/100` persisted;
- candidate preferred `43`, legacy `13`, ties `44`;
- candidate preference on decisive pairs: `76.7857%` (gate `>=75%`);
- full Phase-4 gate: passed.

The exact-font lane attempted `46` OCR-qualified lines on this custom-logo
corpus and admitted `0`; therefore it changed `0/100` outputs and the completed
font-free court is byte-semantically reusable. The report exposes, rather than
hides, the optional refinement cost: p95 `166.99 ms` after T2 and p95
`327.12 ms` from start to optional completion. An owned Arial-Bold fixture
proves that a truly retrievable exact font is admitted through the same wall.

## Phase 5 — passed

Implemented before selection in one bounded CMIR:

- whole-shape candidates for circle/ring, ellipse, rectangle/rounded rectangle,
  triangle/quadrilateral, star, D/bullet and generic free curves;
- type-specific native-lattice fit walls covering support, angular evidence,
  covariance, topology, digital-preimage feasibility and render residual;
- repeated-parameter group macros with explicit members, shared scale,
  residuals and MDL saving, competing against independent members;
- centerline stroke graphs with nodes, edges, width profiles, caps, joins,
  dash/marker/z-order fields and single/polyline/frame/axes/grid/network types;
- solid, linear, radial, translucent and flat-albedo/smooth-shade appearance
  models fit after excluding uncertain edge mixture; palette remains a late
  constraint;
- keep/remove/restore/attach/preserve codec-detail counterfactuals, all scored
  under one frozen image-level degradation posterior; residuals never mutate
  the selected program directly;
- parallel bounded generation and one exact-cover extractor path retaining the
  mandatory V-ICE Best/atomic fallback.

Canonical Experiment 5 report: `benchmarks/pcdc_experiment5/report.json`.

- all seven required stress cases executed, including 500 tiny components,
  200 glyph fragments, 100 dashed segments, 50 overlapping proposals and q30;
- no timeout or OOM;
- all per-lane and total candidate limits respected;
- all three paired scale probes passed the near-linear structural-work gate;
- fallback feasible on every case;
- full Experiment-5 gate: passed.

Experiment 5 also caught and forced a real CMIR fix: final analytic program
parameters are now bound into the draft identity, so distinct programs over
the same coarse REIR cells cannot alias and silently deduplicate. The original
q30 stress exposed a `29.54 s` stroke tail. Phase 10 removed accidental dense
threshold skeletonization and full-canvas adjacency scans; production runtime
allocations are measured by the end-to-end report below. The rerun reduced the
default q30 Phase-5 time to `3.45 s` and its stroke lane to `1.47 s`.

## Phase 6 — passed

Implemented finalized-visible-support-first layer inference, typed contour and
T-junction cues, a confidence-weighted sparse DAG with deterministic cycle
rejection, shape-only hidden completion, transparent/uniform/border/knockout
background hypotheses and one full layered rerender. Owner digests before and
after the layer pass prove that hidden reasoning cannot mutate VSIR ownership.

## Phase 7 — passed

Implemented sparse trust-region continuous refinement with evidence-interval,
SDF/render, stroke/interface, text-grammar and group-consensus factors;
physical-arclength contour weights; covariance from the local information
matrix; frozen macro identities/owners/layer order; native rerender; and
per-macro plus global transactional rollback. An empty-ridge stroke fallback
uses its physical selected width, never an optimization-vector index.

## Phase 8 — passed

Implemented DPIR/XIR with analysis-carrying nodes and SVG/EPS/PDF/DXF/PNG
adapters, plus bounded guarded post-selection abstraction. Exact rewrites cover
Repeat/Map, glyph prototypes, Ring, Mirror, rotational groups, shared symbols,
Bezier/arc and rounded-rectangle patterns, layer commutation and
cutout/stacked representations. A near-circle FreeCurve is explicitly
forbidden from becoming a Circle after selection.

## Phase 9 — passed construction gate, calibration width disclosed

Implemented the PyTorch query ProposalNet, Hungarian multi-head objective,
classical-query union, all ten hard-negative families/ranker and finite-sample
split conformal sets. Canonical report:
`benchmarks/pcdc_experiment9/report.json`.

- split: `210` train / `46` calibration / `44` test;
- hybrid Support Recall@32 class floor: `99.4511%`;
- hard-negative pairwise accuracy: `100%`;
- measured test conformal coverage: `100%` in all six families;
- individual-query IoU50 diagnostic remains only `44.44%` for small shapes and
  `66.67%` for strokes; official recall is the certified classical+neural union.

The current 46-locus calibration split cannot yield non-vacuous 99% finite
sample thresholds: all six thresholds correctly expand to `1.0`. This is
valid coverage but remains a Phase-12 production-freeze blocker until the
planned 2k–5k manually reviewed real loci exist.

## Phase 10 — passed

Implemented a persistent compiler service with a warm ProposalNet worker,
bounded CPU pool and supervised recycling, content-addressed one-pass REIR,
font/glyph LRU cache, persistent exact ROI atlas, Fast/Balanced/Max budgets and
complete valid T0–T4 checkpoints. A dependency-free Rust core is active for
packed CMIR conflict bitsets, circle SDF and deterministic atlas packing.

Canonical Experiment 10 report:
`benchmarks/pcdc_experiment10/report.json` (12 real reviewed loci per mode,
cold evidence cache per mode, warm persistent ProposalNet).

- Fast: p50 `531.68 ms`, p95/max `1629.25 ms`;
- Balanced: p50 `823.87 ms`, p95/max `2534.36 ms`;
- Max: p50 `1040.74 ms`, p95/max `3179.35 ms`;
- all `36/36` requests valid, native backend active, zero minute tails;
- an expired deadline returns a complete T1 hierarchy scene, not partial work.

## Phase 11 — passed BUILD_FREEZE construction

`PCDC_BUILD_FREEZE.json` seals every `vice_compiler` source file, Rust source
and release DLL, ProposalNet checkpoint, canonical real-locus annotations,
both human-court manifests and answer stores, renderer sources, canonical
Experiment 1–10 reports, runtime quality budgets, court weights and conformal
calibration. The independent verifier passes with freeze hash
`ba04530adf32a656ae8b44e8ba90169a3d23217752f647a06d6b66af197608b6`.

The ledger is complete but correctly records `promotion_ready=false`: Phase 12,
non-vacuous 99% calibration and locked blind VAI parity remain explicit
promotion blockers. Any frozen input change requires a new freeze and a full
campaign; thresholds may not be tuned after looking at Phase-12 results.

## Not implemented yet

Phase 12 remains open: the one complete synthetic/renderer/real-locus/VAI/
human/export campaign. VAI parity is not claimed before that gate closes.

## Verification

- `python -m compileall -q vice_compiler web_preview/server.py test_pcdc_court.py` — passed.
- `python -X utf8 -m unittest -v test_pcdc_phase0.py test_pcdc_reir.py test_pcdc_cmir.py test_pcdc_court.py test_pcdc_text.py test_font_match.py test_pcdc_phase5.py` — 50/50 passed, including opaque-SVG, lazy-materialisation, exact-font, whole-shape, stroke, appearance, cleanup and integrated-registry regressions.
- Full 300-locus Experiment 1, 1B and 2 reruns after GT adapter v4 — all gates passed.
- Canonical Experiment 4 rerun with bounded exact-font anytime refinement — full gate passed.
- Experiment 3 rerun after true-SDF/proof-binding changes — machine gate passed.
- Experiment 5 full seven-case stress plus three scale probes — full gate passed.
- Phase 6–10 regression suite — 25/25 passed.
- Full PCDC Phase 0–10 suite — 62/62 passed.
- Experiment 10 real-locus runtime campaign — 36/36 valid and every
  Fast/Balanced/Max speed gate passed.
- BUILD_FREEZE verifier — passed; deliberate promotion blockers preserved.
- Certificate API — 35 public cases, 35 answers, no private label key.
- A SVG, B SVG and source PNG for the first case — HTTP 200.
- POST dry-run — passed and persisted answer count remained 0.
- In-app browser — page loaded, next/previous navigation passed, no console warnings/errors.
